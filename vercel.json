{
  "buildCommand": "npm_config_platform=linux npm_config_arch=x64 npm_config_libc=glibc npm install && npm run build",
  "framework": "vite",
  "outputDirectory": "dist",
  "installCommand": "npm install --platform=linux --arch=x64"
}
